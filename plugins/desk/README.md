## Example messages to post:

### New case:

    {
      "type": "new_case",
      "user": "{{case.customer.name}}", 
      "id": "{{case.id}}",
      "url": "{{case.direct_url}}",
      "subject": "{{case.subject}}"
    }

### New comment:

Still unimplemented.